using FullTextSearch;

class Program
{
    static void Main()
    {
        DocReader.readDocs();
        // build inverted index with documents
        // search func
        // input  + call search function
    }
}